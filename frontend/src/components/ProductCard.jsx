// This file serves as a legacy wrapper for the new ProductCard component
// to maintain backward compatibility with existing code

import ProductCardComponent from "./product/ProductCard";

// Re-export the component with the same name
export const ProductCard = ProductCardComponent;
